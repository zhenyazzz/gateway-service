# Профиль интеграционных тестов
spring:
  webflux:
    problemdetails:
      enabled: true
  main:
    banner-mode: "off"

app:
  idempotency:
    routes:
      - method: POST
        path-prefix: /api/gateway/register
      - method: DELETE
        path-prefix: /api/gateway/delete
  security:
    auth-service-path-prefix: /api/auth/
    whitelist-paths:
      - /api/auth/login
      - /api/auth/register
      - /actuator/health
      - /api/gateway/register
  jwt:
    secret: "Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24tdGhpcy1pcy1hLTI1Ni1iaXQtc2VjcmV0LWtleS1mb3ItZGV2LW9ubHktY2hhbmdlLWl0"
    issuer: "auth-service"

resilience4j:
  retry:
    instances:
      authService:
        maxAttempts: 1
      userService:
        maxAttempts: 1
      orderService:
        maxAttempts: 1
